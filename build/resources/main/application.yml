spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tokyo_spots?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: mari
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create  # update에서 create로 변경
    show-sql: true
    properties:
      hibernate:
        format_sql: true